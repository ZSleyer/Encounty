name: Release

on:
  push:
    tags:
      - 'v*'

permissions:
  contents: write

jobs:
  build:
    name: Build & Release
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v4
        with:
          fetch-depth: 0

      - name: Set up Go
        uses: actions/setup-go@v5
        with:
          go-version: '1.24'
          cache-dependency-path: backend/go.sum

      - name: Set up Node.js
        uses: actions/setup-node@v4
        with:
          node-version: '22'

      - name: Set version from tag
        run: |
          TAG_VERSION=${GITHUB_REF#refs/tags/}
          CLEAN_VERSION=$(echo "$TAG_VERSION" | sed 's/^v//')
          if [[ "$CLEAN_VERSION" != *.*.* ]]; then
            CLEAN_VERSION="${CLEAN_VERSION}.0"
          fi
          echo "VERSION=$TAG_VERSION" >> $GITHUB_ENV
          echo "CLEAN_VERSION=$CLEAN_VERSION" >> $GITHUB_ENV

      - name: Install Frontend Dependencies
        working-directory: frontend
        run: yarn install

      - name: Set Frontend Version
        working-directory: frontend
        run: npm pkg set version="$CLEAN_VERSION"

      - name: Build Frontend
        working-directory: frontend
        run: yarn build

      - name: Install go-winres
        run: go install github.com/tc-hib/go-winres@latest

      - name: Generate Icons
        run: |
          cd backend
          go run ../scripts/generate_icons.go

      - name: Build Linux Binary
        working-directory: backend
        env:
          GOOS: linux
          GOARCH: amd64
          CGO_ENABLED: 0
        run: |
          mkdir -p ./frontend
          cp -r ../frontend/dist ./frontend/
          go build -ldflags="-s -w -X main.version=$VERSION -X main.buildDate=$(date -u +%d.%m.%y)" -o ../encounty-linux main.go
          rm -rf ./frontend

      - name: Build Windows Binary
        working-directory: backend
        env:
          GOOS: windows
          GOARCH: amd64
          CGO_ENABLED: 0
        run: |
          WIN_VER=$(echo "$CLEAN_VERSION" | awk -F. '{if(NF==2) print $0".0"; else print $0}')
          $(go env GOPATH)/bin/go-winres make --product-version "${WIN_VER}.0" --file-version "${WIN_VER}.0"
          mkdir -p ./frontend
          cp -r ../frontend/dist ./frontend/
          go build -ldflags="-s -w -X main.version=$VERSION -X main.buildDate=$(date -u +%d.%m.%y) -H=windowsgui" -o ../encounty-windows.exe main.go
          rm -rf ./frontend
          rm -f *.syso

      - name: Compress Linux Binary
        run: |
          sudo apt-get update && sudo apt-get install -y upx
          upx --best --lzma encounty-linux || echo "UPX failed, continuing..."

      - name: Build Electron
        working-directory: electron
        run: |
          yarn install
          npm pkg set version="$CLEAN_VERSION"
          yarn build

      - name: Package Electron Linux
        working-directory: electron
        env:
          GH_TOKEN: ${{ secrets.GITHUB_TOKEN }}
        run: yarn package:linux

      - name: Create Release
        uses: softprops/action-gh-release@v2
        with:
          files: |
            encounty-linux
            encounty-windows.exe
            electron/release/Encounty.AppImage
          generate_release_notes: true
