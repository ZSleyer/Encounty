/**
 * templateEditorTypes.ts -- Types shared by the template editor modules.
 */

/** Step of the template creation flow the editor is currently showing. */
export type Phase = "video" | "replay" | "snapshot" | "test" | "confirm";
